@model WMS.SKU
@{
    ViewBag.Title = "Index";
}


<h5 class="text-primary">Recieve</h5>
<hr />
<table class="table table-striped">
    <tr>
        <th class="text-primary">
            SKU
        </th>
        <th class="text-primary">
            Description
        </th>
        <th class="text-primary">
            Photo
        </th>
        <th class="text-primary">
            Category
        </th>
        <th class="text-primary">
            Supplier
        </th>
        <th class="text-primary">
            ASN Qty
        </th>
        <th class="text-primary">
            Good
        </th>
        <th class="text-primary">
            Damage
        </th>
        <th class="text-primary">
            Short
        </th>
        <th class="text-primary">
            Over
        </th>
        <th class="text-primary">
            Total Recieved
        </th>
        <th class="text-primary">
            Total SO
        </th>
        <th class="text-primary">
            Total BO
        </th>
        <th class="text-primary">
            Total WMS
        </th>
    </tr>

    @*@foreach (var item in Model)
        {
            <tr>
                <td class="text-primary">
                    <a href="@Url.Action("Details", "SKUs",new { id = item.SKU1 })">
                        @item.SKU1
                    </a>
                    <br />
                    <img src="@Url.Content(item.image_url)" alt="Image" height="90" width="90" />

                </td>
                <td class="text-primary">
                    @item.description
                </td>
                <td class="text-primary">
                    @item.client_name
                </td>
                <td class="text-primary"></td>
                <td class="text-primary"></td>
                <td class="text-primary">
                    @item.category
                </td>
                <td class="text-primary">
                    @item.supplier
                </td>
                <td class="text-primary">
                    @item.cost
                </td>

                <td class="text-primary">
                    <a href="@Url.Action("Edit", "SKUs",new { id = item.SKU1 })">
                        <i class="glyphicon glyphicon-edit"></i>
                    </a>
                </td>
                <td class="text-primary">

                    <a href="@Url.Action("Delete", "SKUs",new { id = item.SKU1 })">
                        <i class="glyphicon glyphicon-trash"></i>
                    </a>
                </td>

            </tr>
        }*@

</table>
<div class="form-group">
    <div class="col-md-offset-2 col-md-10">
        <input type="reset" value="Cancel" class="btn btn-primary" />
        <input type="submit" value="Send Acknowledgement" class="btn btn-larger btn-success" id="submit" />
    </div>
</div>
